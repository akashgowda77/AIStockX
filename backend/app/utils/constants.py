# Centralized constants for the application.

APP_NAME = "AIStockX"
API_VERSION = "v1"

# Authentication / JWT
JWT_EXPIRATION_MINUTES_DEFAULT = 60

# ML defaults (used later)
DEFAULT_PREDICTION_DAYS = 7

# Default chart settings (used later)
DEFAULT_CHART_WIDTH = 900
DEFAULT_CHART_HEIGHT = 500

